# Profile Retouch Studio v2.7 업데이트 계획서

## 핵심 목표
이번 버전은 **편집 조작 버튼 접근성**과 **저장 결과의 화질/색상 일치도**를 최우선으로 개선합니다.

---

## 1. 재생 / 정지 / 미리보기 / 저장 버튼 상단 이동
### 문제
기존 버전에서는 주요 조작 버튼이 미리보기 아래쪽에 있어 화면을 내려야 하는 경우가 있었습니다.

### 개선
- 미리보기 캔버스 바로 위에 **상단 고정 툴바** 추가
- 툴바에는 다음 항목 배치
  - 재생
  - 정지
  - 편집 미리보기 ON/OFF
  - 현재 시간
  - 화질 프리셋
  - 저장 버튼
  - 플레이헤드
- 스크롤해도 툴바가 상단에 유지되도록 sticky 처리

---

## 2. 미리보기와 저장 결과의 렌더 방식 통일
### 문제
미리보기는 900×900 캔버스에서 렌더링하고 저장은 1000~2000px 캔버스에서 다시 렌더링해 결과가 다르게 보일 수 있었습니다.

### 개선
- 미리보기 자체를 **최종 출력 크기와 동일한 내부 Master Canvas**에서 먼저 렌더링
- 화면에서는 Master Canvas를 900×900 미리보기로 축소 표시
- 저장도 동일한 `renderScene()` 로직과 동일한 출력 해상도를 사용
- 고품질 image smoothing 적용
- sRGB 2D Canvas 컨텍스트를 우선 사용

즉,

`최종 출력 크기 렌더 → 화면 축소 미리보기`

구조로 변경하여 크기 차이 때문에 발생하는 효과 크기/배치/선명도 차이를 줄입니다.

---

## 3. 영상 비트레이트 대폭 상향
기본 저장 품질을 기존보다 크게 높입니다.

### 선택 가능한 품질
- 고화질: 30 Mbps
- 미리보기 우선: 50 Mbps (기본)
- 초고화질: 80 Mbps

용량보다 화질을 우선하도록 설계합니다.

---

## 4. 프레임 캡처 안정화
### 개선
- 가능한 브라우저에서는 CanvasCaptureMediaStreamTrack의 `requestFrame()` 방식 사용
- 각 프레임을 지정 FPS 기준 시간에 맞춰 직접 렌더링
- 저장 중 임의의 중간 프레임이 캡처되는 문제를 줄임

---

## 5. 색상 변질 최소화
- Preview와 Export 모두 동일한 배경/필터/색조/레이어 렌더 코드를 사용
- sRGB Canvas context 우선 사용
- MP4/H.264 사용 가능 시 고비트레이트 MP4 우선
- 브라우저가 MP4 MediaRecorder를 지원하지 않을 때만 WebM fallback

> 참고: 브라우저의 H.264/MP4 인코더 자체가 RGB를 YUV 영상 색공간으로 변환하기 때문에 아주 미세한 색 변화까지 100% 제거하는 것은 브라우저별로 제한이 있습니다. v2.7은 렌더링 차이와 과도한 압축으로 인한 변화를 최대한 줄이는 방향입니다.

---

## 6. Export 시 편집용 Ghost Preview 제외
편집 미리보기 기능으로 보이는 흐린 선택 레이어는 저장 결과에는 절대로 들어가지 않도록 분리합니다.

---

## 유지되는 기능
- 사진 다중 레이어
- 귀여운 장식 24종
- PNG 장식 레이어
- 간단 타임라인
- 효과 미리보기
- 나침반 방향 선택
- 원형 프레임 PNG
- 최대 2000×2000 1:1 출력
