# Profile Retouch Studio v2.9 업데이트 계획서

## 핵심 수정 목표
v2.8에서 60FPS를 추가한 뒤 저장할 때 **5초 프로젝트가 약 10초로 늘어나고, 모든 움직임이 약 2배 느려지는 문제**를 수정합니다.

원인은 영상의 프레임 시간과 실제 녹화 시간이 서로 다르게 계산되던 데 있습니다.

기존 방식은 매 프레임마다:

1. 고해상도 프레임 렌더링
2. 60FPS 기준 16.67ms 추가 대기

를 순서대로 수행했습니다.

예를 들어 한 프레임 렌더링에 약 16ms가 걸리면 실제로는
`16ms 렌더링 + 16.67ms 대기 ≈ 32ms`
가 필요해져 60FPS가 아니라 약 30FPS 속도로 실시간 녹화되었습니다.
MediaRecorder는 실제 경과 시간을 기록하므로 결과 영상 길이가 거의 두 배가 되고 모션도 절반 속도로 재생되는 문제가 생겼습니다.

---

## v2.9 해결 방식

### 1. Wall-clock 기반 저장 시간축
- 저장 중 애니메이션 시간은 `frameIndex / FPS`만 고정적으로 사용하지 않고 **실제 경과 시간**을 기준으로 계산합니다.
- 프로젝트가 5초면 실제 녹화도 약 5초 시점에서 종료합니다.

### 2. 렌더링 시간을 대기시간에서 차감
예를 들어 60FPS의 한 프레임 간격은 약 16.67ms입니다.

- 렌더링 5ms → 약 11.67ms만 추가 대기
- 렌더링 12ms → 약 4.67ms만 추가 대기
- 렌더링 20ms → 추가 대기 없이 다음 현재시간 프레임 렌더링

즉 렌더링에 사용한 시간을 다시 한 번 기다리지 않습니다.

### 3. 느린 PC에서도 영상 속도 유지
2000×2000 고해상도 렌더링에서 컴퓨터가 60개의 모든 프레임을 실시간으로 계산하지 못하는 경우:

- 영상을 10초로 늘이지 않음
- 애니메이션을 절반 속도로 만들지 않음
- 대신 필요한 경우 중간 프레임을 일부 건너뛰어 **5초는 5초로 유지**

### 4. 저장 중 다른 미리보기 애니메이션 정지
- 메인 재생을 저장 시작 시 일시 정지
- 효과/장식 소형 미리보기 렌더링도 저장 중 중단
- 저장에 GPU/CPU를 더 집중하도록 조정

### 5. 저장 후 작업 상태 복원
- 저장이 끝나면 메인 캔버스를 다시 정상 편집 상태로 복원
- 저장 전 재생 중이었다면 재생 상태도 다시 시작

---

## 유지되는 기능
- 최대 2000×2000
- 1:1 비율
- 60FPS 선택
- 30 / 50 / 80Mbps 품질 프리셋
- Undo / Ctrl+Z
- 사진 다중 레이어
- 귀여운 장식 24종
- PNG 장식
- 원형 프레임 PNG
- 상단 고정 재생/저장 툴바
