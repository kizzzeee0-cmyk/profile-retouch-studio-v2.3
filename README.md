# Profile Retouch Studio v2.5

브라우저에서 바로 열 수 있는 **단일 HTML 기반 프로필 모션 제작 도구**입니다.

## 주요 기능
- 사진 여러 장을 배경 위에 겹치기
- 텍스트 이미지에 어울리는 다양한 등장효과
- 귀여운 장식 24종 레이어
- 업로드한 PNG를 장식처럼 여러 개 뿌리기
- 원형 프레임 PNG 업로드 및 회전
- 1:1 비율 유지
- MP4 우선 저장 (미지원 브라우저는 WebM 대체)

## 사용 방법
1. `index.html`을 브라우저에서 엽니다.
2. 배경을 설정합니다.
3. `+ 사진`, `+ 귀여운 장식`, `+ PNG 장식` 버튼으로 레이어를 추가합니다.
4. 레이어 목록에서 하나를 선택해 오른쪽 편집 패널에서 속성을 수정합니다.
5. 재생으로 확인한 뒤 `MP4 저장`을 눌러 내보냅니다.

## 배포
정적 파일만 사용하므로 GitHub Pages / Cloudflare Pages에 그대로 업로드해 사용할 수 있습니다.
